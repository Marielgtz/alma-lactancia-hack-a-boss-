/* Ajustar el contenedor de los botones */
.rbc-btn-group {
  position: relative;
  display: flex;
  justify-content: center;
  margin-bottom: 1rem;
}

/* Ajustar cada botón individualmente */
.rbc-btn-group button {
  flex: 1 1 auto; /* Permite que los botones crezcan y se encojan según el espacio disponible */
  min-width: 80px;
  padding: 0.5rem;
  font-size: 0.9rem;
  text-align: center;
  white-space: nowrap; /* Evita el desbordamiento del texto en múltiples líneas */
}